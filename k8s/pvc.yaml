apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: runner-models
  namespace: llmmll
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: nfs-client